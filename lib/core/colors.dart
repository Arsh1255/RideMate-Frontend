import 'package:flutter/material.dart';

class AppColors {
  // PRIMARY
  static const primaryBlue = Color(0xFF00BFFF);
  static const primaryGreen = Color(0xFF39D35F);

  // TEXT
  static const textPrimary = Color(0xFF0F172A);
  static const textSecondary = Color(0xFF64748B);
  static const textBold = Color(0xFF4B5563);

  // borders and error
  static const border = Color(0xFFE2E8F0);
  static const error = Color(0xFFEF4444);

  // CARD
  static const borderBlue = Color(0xFF04C0F2);

  // INFO BOX (ACEDFF @ 48%)
  static const infoBlue = Color(0x7AACEDFF);

  // ADDITIONAL INFO BAR (19C5FF @ 24%)
  static const infoLight = Color(0x3D19C5FF);
  static const infoText = Color(0xFF606D83);

  // STATUS
  static const pendingBg = Color(0xFFFEF3C7);
  static const pendingText = Color(0xFF92400E);

  static const removedText = Color(0xFFEF8B58);

  static const background = Color(0xFFF8FAFC);


}